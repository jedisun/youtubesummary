# YouTube 直播抓流验证 Demo 设计

## 1. 目标

本设计文档只关注直播方案中的“输入层验证”。

目标是在当前环境中验证以下问题：

1. 是否能够从 YouTube 直播链接稳定解析出可用音频流
2. 是否能够使用 `ffmpeg` 按固定时长持续切出音频 chunk
3. 在有限时长内，是否能够稳定产出可供后续转写使用的音频文件

本阶段**不接入**：

- `faster-whisper`
- OpenAI 摘要
- token 统计
- 直播状态恢复全量实现

---

## 2. 方案结论

采用以下最小验证链路：

**YouTube Live URL -> yt-dlp 解析直播流 -> ffmpeg 切片 -> 本地 chunk 文件**

职责划分：

- `yt-dlp`：处理 YouTube 直播页面解析，拿到实际媒体流地址
- `ffmpeg`：读取流并按固定长度切成音频片段
- `live_capture_demo.py`：负责流程编排、日志记录、运行时控制和结果汇总

---

## 3. 验证范围

验证脚本需要具备以下能力：

1. 接收一个 YouTube 直播链接
2. 解析直播流地址
3. 启动 `ffmpeg` 连续切片
4. 在指定时长后自动结束采集
5. 输出 chunk 数量、总时长、输出目录、日志文件路径

---

## 4. 关键设计

### 4.1 输入参数

建议支持：

- `url`：YouTube 直播链接
- `--output-dir`：输出目录，默认 `downloads/live`
- `--chunk-seconds`：切片时长，默认 `30`
- `--run-seconds`：总运行时长，默认 `600`
- `--audio-format`：输出音频格式，默认 `wav`

### 4.2 流获取方式

不直接让 `yt-dlp` 持续下载完整文件，而是先解析直播直链。

优先方式：

1. 使用 Python `yt_dlp` 在 `download=False` 模式下提取信息
2. 优先从提取结果中选择纯音频流
3. 如果直播源没有纯音频流，则回退到最低码率的可播放 HLS 变体
3. 把直链交给 `ffmpeg`

### 4.3 切片方式

`ffmpeg` 使用 `segment muxer` 切片，推荐参数：

- 开启 `reconnect`
- 音频转为单声道 `16kHz`
- 输出固定时长 chunk
- 文件名带时间戳，便于排序和调试

输出路径建议：

```text
downloads/live/
├─ chunks/
└─ logs/
```

chunk 文件名建议：

```text
{run_stamp}_chunk_00001.wav
```

---

## 5. 验收标准

通过标准：

1. 连续运行至少 10 分钟
2. 稳定生成多个 chunk 文件
3. 日志中无持续性 fatal 错误
4. 至少随机抽查 1 个 chunk 可被 `ffprobe` 正常识别

建议观测指标：

- `chunk_count`
- `captured_seconds`
- `wall_clock_seconds`
- `ffmpeg_exit_code`
- `stream_url_resolved`

---

## 6. 已知边界

- 直播流地址可能过期
- 不同直播源质量与分片方式不同
- 网络抖动可能导致中途断流
- 第一版 demo 不处理自动续连到新解析 URL
- 第一版 demo 只验证抓流与切片，不验证后续转写和摘要质量

---

## 7. 与主项目的关系

该 demo 是直播模式的前置验证，不替代主流程设计。

验证通过后，再继续实现：

1. chunk 轮询
2. 增量转写
3. 窗口级摘要
4. 状态恢复

对应代码建议放在：

- `scripts/live_capture_demo.py`

后续若验证通过，再把能力迁移到：

- `src/youtubesummary/live/`
